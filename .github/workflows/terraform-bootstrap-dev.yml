name: Terraform bootstrap dev

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  authorize-ref:
    name: Authorize development bootstrap revision
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Enforce develop
        shell: pwsh
        run: |
          if ($env:GITHUB_REF -ne 'refs/heads/develop') {
            throw "El bootstrap dev solo puede ejecutarse desde refs/heads/develop."
          }

  bootstrap:
    name: Provision isolated dev bootstrap
    needs: authorize-ref
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    environment: iac-bootstrap-dev
    concurrency:
      group: terraform-bootstrap-dev
      cancel-in-progress: false
    permissions:
      contents: read
      id-token: write
    env:
      PROJECT_NAME: vetsoftware
      AWS_REGION: ${{ vars.AWS_REGION || 'us-east-1' }}
      AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
      AWS_ROLE_TO_ASSUME: ${{ vars.AWS_BOOTSTRAP_ROLE_ARN }}
      GITHUB_ORGANIZATION: ${{ github.repository_owner }}
      GITHUB_ORGANIZATION_ID: ${{ vars.GITHUB_ORGANIZATION_ID }}
      GITHUB_REPOSITORY_IDS_JSON: ${{ vars.GITHUB_REPOSITORY_IDS_JSON }}

    steps:
      - name: Check out authorized bootstrap revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: 1.15.8
          terraform_wrapper: false

      - name: Validate bootstrap identity configuration
        shell: pwsh
        run: |
          foreach ($name in @('AWS_ACCOUNT_ID', 'AWS_ROLE_TO_ASSUME', 'GITHUB_ORGANIZATION_ID', 'GITHUB_REPOSITORY_IDS_JSON')) {
            if ([string]::IsNullOrWhiteSpace([Environment]::GetEnvironmentVariable($name))) {
              throw "Configure $name en iac-bootstrap-dev."
            }
          }

      - name: Configure isolated dev bootstrap credentials
        uses: aws-actions/configure-aws-credentials@451ce2a72e8d729a59ebeaacab82dbce58e7af4c # v6
        with:
          role-to-assume: ${{ env.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}
          allowed-account-ids: ${{ env.AWS_ACCOUNT_ID }}
          role-session-name: vetsoftware-bootstrap-dev-${{ github.run_id }}

      - name: Bootstrap dev with remote state
        shell: pwsh
        run: ./.github/scripts/terraform-bootstrap.ps1 -Environment dev
